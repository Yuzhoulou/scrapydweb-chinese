启动命令： python D:\scrapydweb\scrapydweb-master\scrapydweb\run.py

配置文件：scrapydweb-master/scrapydweb/default_settings.py

连接 scrapyd：D:\scrapydweb\scrapydweb-master\scrapydweb\views\baseview.py


fastapi 和 5000 用一个端口启用。

调试过程：
 docker run -d -p 6800:6800 -p 5000:5000 scrapydweb-public_sentiment-image:latest /bin/bash
apt-get update && apt-get install -y iputils-ping net-tools curl grep
apt-get install -y procps

确保 Scrapyd 绑定 0.0.0.0 以接受外部连接（主机访问）：
cd /code/gc_public_sentiment
echo "[scrapyd]" > scrapyd.conf
echo "bind_address = 0.0.0.0" >> scrapyd.conf
echo "http_port = 6800" >> scrapyd.conf

后台启动：
scrapyd > scrapyd.log 2>&1 &

查看日志：
cat scrapyd.log

确认进程：
ps aux | grep scrapyd

验证端口：
netstat -tuln | grep 6800

测试容器内连接：
curl http://127.0.0.1:6800

测试主机访问：
http://127.0.0.1：6800

启动scrapydweb（绑定0.0.0.0）

cat /code/gc_public_sentiment/scrapydweb_settings_v11.py

sed -i "s/SCRAPYDWEB_BIND = '127.0.0.1'/SCRAPYDWEB_BIND = '0.0.0.0'/" /code/gc_public_sentiment/scrapydweb_settings_v11.py

export SCRAPYDWEB_SETTINGS_PY=/code/gc_public_sentiment/scrapydweb_settings_v11.py
scrapydweb --bind 0.0.0.0 --port 5000 > scrapydweb.log 2>&1 &

ps aux | grep scrapydweb

cat scrapydweb.log

netstat -tuln | grep 5000

curl http://127.0.0.1:5000

http://127.0.0.1:5000/

出现marshmallow报错
pip install marshmallow==3.26.1

将scrapyd和scrapydweb绑定0.0.0.0而不是127.0.0.1:
DOCKER网络环境隔离
Docker 容器运行在独立的网络命名空间中，容器的 127.0.0.1 是容器本地的环回地址，与主机的 127.0.0.1 不同。
如果 Scrapyd 或 ScrapydWeb 绑定到 127.0.0.1，它们只接受容器内部的连接（例如容器内的 curl http://127.0.0.1:6800），而拒绝外部连接（例如主机或其他容器的请求）。

端口映射
将容器的6800和5000端口映射到主机对应的端口
docker的端口映射会将主机的请求（例如http://127.0.0.1:6800)转发到容器相应端口，前提是容器内的服务监听的地址允许外部链接
绑定127.0.0.1可能会导致转发失败，绑定0.0.0.0确保服务监听所有网络接口，包括DOCKER桥接网络接口


项目改进
1.scrapydweb使用docker重新启动后不会自动保存爬虫队列，需要手动再次添加启动

2.部署进程中，下载依赖pymilvus==2.4.7时 PIP 会忽略本地环境里的marshmallow-3.26.1-py3-none-any.whl，
  自动从 PyPi下载最新版本的marshmallow==4.0.0。导致与其他依赖版本冲突。(在dockerfile里已经修改强行安装本地的marshmallow依赖)
  或者在容器内下载pip install marshmallow==3.26.1

3.docker run -d -p 6800:6800 -p 5000:5000 --env.file dev.env <image_name>
  启动docker容器需要开放两个端口6800和5000，6800为scrapyd开放的端口，5000为scrapydweb开放的端口
  添加环境文件dev.env 确保FORCE_ENV=DEV

4. exec logparser -dir /code/gc_public_sentiment/logs 启用logparser

5. 需要将scrapydweb前端修改为中文，使用setup.py修改所需依赖的版本，再在本地生成scrapydweb-1.6.0-py3-none-any.whl， python setup.py bdist_wheels。
   在dockerfile里修改强行安装本地的scrapydweb.whl。避免依赖版本冲突。

6. 有冲突的依赖版本idna==2.7 flask==2.0.0